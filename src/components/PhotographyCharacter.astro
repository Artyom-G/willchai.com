<span class="photographyCharacter" aria-hidden="true">
  <span class="photographyCharacterSprite"></span>
</span>

<style>
  .photographyCharacter {
    --sprite-size: 64px;
    position: relative;
    display: block;
    flex: 0 0 auto;
    width: calc(var(--sprite-size) * 1.5);
    height: var(--sprite-size);
    overflow: hidden;
    pointer-events: none;
  }

  .photographyCharacterSprite {
    position: absolute;
    left: calc(var(--sprite-size) * 0.5);
    width: var(--sprite-size);
    height: var(--sprite-size);
    background: url("/photography/pixel-photographer.png") 100% 0 / 400% 100% no-repeat;
    image-rendering: pixelated;
    animation: photographerEntrance 4s steps(1, end) 1 both;
  }

  @keyframes photographerEntrance {
    0% {
      background-position-x: 0%;
      transform: translateX(-75%);
    }
    25% {
      background-position-x: 33.333333%;
      transform: translateX(-37.5%);
    }
    50% {
      background-position-x: 66.666667%;
      transform: translateX(0);
    }
    75%, 100% {
      background-position-x: 100%;
      transform: translateX(0);
    }
  }

  @media (max-width: 46rem) {
    .photographyCharacter { --sprite-size: 32px; }
  }

  @media (prefers-reduced-motion: reduce) {
    .photographyCharacterSprite { animation: none; }
  }
</style>
